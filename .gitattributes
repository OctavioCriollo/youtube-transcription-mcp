# Shell scripts run as Linux container entrypoints — a CRLF shebang (`#!/bin/sh\r`)
# makes the kernel fail to find the interpreter. Force LF regardless of the OS
# the commit is made from.
*.sh text eol=lf
